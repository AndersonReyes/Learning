#include "exercise.h"

// All five exercises are function templates, fully defined in exercise.h --
// template definitions must be visible at the point of instantiation, so
// there's nothing to put in a separate translation unit. This file exists
// only so the topic's build command
// (g++ ... exercise_test.cpp exercise.cpp && ./test) stays the same shape as
// every other topic.
