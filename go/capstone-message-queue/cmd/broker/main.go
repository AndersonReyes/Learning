// Command broker is the message queue server binary.
// Implement the server package, then wire it up here.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "not implemented")
	os.Exit(1)
}
